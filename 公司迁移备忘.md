# Octlex 公司账号迁移备忘（账号批下来后执行）

## ⚠️ 迁移前的铁律

公司账号未就绪期间，**绝不要在个人账号/团队下构建含 octlex 标识的工程**——
自动签名会把 `iCloud.com.octlex.dailyhexagram` 和 `group.com.octlex.dailyhexagram`
注册到个人账号，而 iCloud 容器与 App Group **永久无法删除**，公司账号将永远用不了它们。

## 一、代码迁移（跟 Claude 说"执行公司迁移"即可，或手动跑）

对以下 10 个文件全局替换 `com.dj.DailyHexagram` → `com.octlex.dailyhexagram`，
并把 pbxproj 里 4 处 `DEVELOPMENT_TEAM = 6AD5WMNPNJ;` 清空：

```
DailyHexagram.entitlements
DailyHexagramWidget.entitlements
DailyHexagram.storekit
DailyHexagram.xcodeproj/project.pbxproj
DailyHexagram/Models/InviteManager.swift
DailyHexagram/Models/StoreManager.swift
DailyHexagram/Models/WidgetBridge.swift
DailyHexagramWidget/DailyHexagramWidget.swift
README.md
ROADMAP.md
```

替换后自动得到：Bundle `com.octlex.dailyhexagram`（Widget 同前缀）、
App Group `group.com.octlex.dailyhexagram`、容器 `iCloud.com.octlex.dailyhexagram`、
内购 ID `com.octlex.dailyhexagram.premium.monthly` 等六个。

## 二、Xcode

1. Settings → Accounts 登录公司 Apple ID
2. 两个 target 的 Signing & Capabilities → Team 选 Octlex（自动注册全部新标识）
3. 真机 ⌘R 验证签名

## 三、CloudKit（新容器从零建）

Dashboard（公司团队）→ `iCloud.com.octlex.dailyhexagram`：

1. 建 5 个记录类型：InviteCode(ownerUserID:String) / Redemption(code:String,
   inviterUserID:String, credited:Int64) / RewardClaim(redemption:String) /
   GiftCode(amount,maxRedemptions,redeemedCount,active:Int64, expiresAt:Date) /
   GiftRedemption(code:String)
2. 索引：Redemption 的 inviterUserID 与 credited 各加 QUERYABLE
   （想在控制台浏览记录再给各类型 recordName 加 QUERYABLE）
3. 开发环境真机跑通：生成邀请码 → 兑换 → 领奖 → 礼品码兑换
4. Deploy Schema to Production
5. Production 建礼品码：gift-REVIEW88（amount=100,active=1）供审核员

## 四、App Store Connect（公司）

1. 第一时间：协议税务银行 → 签 Paid Apps + 公司银行/税表（审批数日）
2. 新建 App（Bundle: com.octlex.dailyhexagram）→ 复制数字 Apple ID
   → 填入 InviteManager.appStoreURLString → 重新打包
3. 建 6 个内购（照上架材料.md §5），订阅组月度/年度
4. 商店文案/截图/隐私问卷/年龄分级 → 全部照上架材料.md
5. 隐私政策 URL：如托管处变更，同步改 StoreManager.LegalLinks.privacyPolicy
6. TestFlight 重新邀请测试员（新 Bundle = 新 App）

## 五、注意

- 旧包（com.dj）本地数据不迁移到新包（KVS 随 TeamID+Bundle 走），测试数据无所谓
- 个人账号的旧 ASC App 记录/标识符可删可留
- 商店"开发者"将显示公司法定名 Octlex
